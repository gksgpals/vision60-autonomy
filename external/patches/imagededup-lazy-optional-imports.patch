diff --git a/imagededup/methods/__init__.py b/imagededup/methods/__init__.py
--- a/imagededup/methods/__init__.py
+++ b/imagededup/methods/__init__.py
@@ -1,2 +1,9 @@
 from .hashing import PHash, AHash, DHash, WHash
-from .cnn import CNN
+
+
+def __getattr__(name):
+    """Load the optional Torch CNN implementation only when requested."""
+    if name == 'CNN':
+        from .cnn import CNN
+        return CNN
+    raise AttributeError(name)
diff --git a/imagededup/methods/hashing.py b/imagededup/methods/hashing.py
--- a/imagededup/methods/hashing.py
+++ b/imagededup/methods/hashing.py
@@ -7,10 +7,8 @@
 from multiprocessing import cpu_count
 import numpy as np
-import pywt
 from scipy.fftpack import dct
 
-from imagededup.handlers.search.retrieval import HashEval
 from imagededup.utils.general_utils import (
@@ -227,6 +225,8 @@ class Hashing:
         """
         logger.info('Start: Evaluating hamming distances for getting duplicates')
 
+        from imagededup.handlers.search.retrieval import HashEval
+
         result_set = HashEval(
@@ -678,6 +678,8 @@ class WHash(Hashing):
         Returns:
             A string representing the wavelet hash of the image.
         """
+        import pywt
+
         # decomposition level set to 5 to get 8 by 8 hash matrix
diff --git a/imagededup/utils/__init__.py b/imagededup/utils/__init__.py
--- a/imagededup/utils/__init__.py
+++ b/imagededup/utils/__init__.py
@@ -1,2 +1,9 @@
-from .plotter import plot_duplicates
-from .models import CustomModel
+def __getattr__(name):
+    """Load optional plotting and Torch model helpers only when requested."""
+    if name == 'plot_duplicates':
+        from .plotter import plot_duplicates
+        return plot_duplicates
+    if name == 'CustomModel':
+        from .models import CustomModel
+        return CustomModel
+    raise AttributeError(name)
